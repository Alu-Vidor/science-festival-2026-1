const assert=require('node:assert/strict');const {simulate,network}=require('../epidemic.js');
const a=simulate({});assert.deepEqual(a,simulate({}));for(const h of a.history)assert.equal(h.S+h.I+h.R,60);assert.equal(a.history.at(-1).I,0);assert.equal(simulate({p:0}).total,1);assert.equal(simulate({edges:[]}).total,1);assert.equal(simulate({edges:[],duration:5}).history.length,6);assert.equal(simulate({p:1,duration:12}).total,60);assert.equal(simulate({travel:false,meeting:false,p:1}).total,10);assert(simulate({travel:false,meeting:false}).activity<100);console.log('Epidemic model checks passed');
