const assert=require('node:assert/strict'),{simulate,destination}=require('../epidemic.js');
const a=simulate({});assert.deepEqual(a,simulate({}));for(const h of a.history){assert.equal(h.S+h.I+h.R,60);assert.equal(h.loc.length,60);for(const e of h.events)assert.equal(h.loc[e.source],h.loc[e.target]);}assert.equal(a.history.at(-1).I,0);assert.equal(simulate({p:0}).total,1);assert.equal(simulate({p:0,duration:5}).history.at(-1).day,5);assert.equal(destination(0,1,1,{school:false,bus:true}),'h0');assert.equal(destination(35,1,1,{bus:false}),'h3');const b=simulate({bus:false,school:false,market:false,park:false});assert(b.activity<a.activity);assert.equal(a.activity,100);console.log('City model checks passed');
